# Environment Variables voor Test Omgeving
# Kopieer deze naar Hostinger hPanel → Node.js Apps → [Test App] → Environment Variables
# Vervang alle placeholder waarden met echte waarden

# Server Configuration
NODE_ENV=staging
PORT=3000

# CORS Configuration
# Vervang met je tijdelijke test URL (zowel http:// als https://)
ALLOWED_ORIGINS=https://jouw-test-url.hostingersite.com,http://jouw-test-url.hostingersite.com

# Admin Configuration
ADMIN_PASSWORD=test_admin_password_123
ADMIN_EMAIL=test@plankjesmaffia.nl

# Database Configuration (optioneel - gebruik aparte test database)
# Als je geen database gebruikt, laat deze leeg of verwijder ze
DB_HOST=localhost
DB_USER=u123456789_admin
DB_PASSWORD=je_test_db_wachtwoord
DB_NAME=u123456789_plankjes_test
DB_PORT=3306

# Email Configuration (optioneel)
# Als je geen email gebruikt, laat deze leeg of verwijder ze
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=test@plankjesmaffia.nl
SMTP_PASSWORD=je_email_wachtwoord
SMTP_FROM=test@plankjesmaffia.nl

# Subdomain Configuration (niet nodig voor tijdelijke URLs)
# Laat deze leeg of verwijder ze voor dev/test omgevingen


# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git!

# ============================================
# Server Configuration
# ============================================
NODE_ENV=production
PORT=3000

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins
# Example: https://example.com,https://www.example.com,https://admin.example.com
ALLOWED_ORIGINS=https://example.com,https://www.example.com

# ============================================
# Admin Configuration
# ============================================
# ⚠️ CHANGE THIS TO A STRONG PASSWORD IN PRODUCTION!
ADMIN_PASSWORD=change_this_to_a_strong_password
ADMIN_EMAIL=info@example.com

# ============================================
# Database Configuration (MySQL)
# ============================================
# Get these values from hPanel → Databases → MySQL Databases
# See HOSTINGER_DATABASE_SETUP.md for detailed instructions

DB_HOST=localhost
# Database user (usually has prefix like u123456789_admin)
DB_USER=your_database_user
# Database password - USE A STRONG PASSWORD!
DB_PASSWORD=your_database_password
# Database name (usually has prefix like u123456789_plankjes)
DB_NAME=your_database_name
DB_PORT=3306
# Optional: Connection pool limit (default: 10)
DB_CONNECTION_LIMIT=10

# ============================================
# Email Configuration (SMTP) - Optional
# ============================================
# If not configured, the app will use mailto: links as fallback
# Get SMTP settings from hPanel → Email → SMTP Settings

SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=info@example.com
SMTP_PASSWORD=your_smtp_password
SMTP_FROM=info@example.com

# ============================================
# Subdomain Configuration - Optional
# ============================================
# Only needed if you use subdomains
ADMIN_SUBDOMAIN=admin.example.com
STAGING_SUBDOMAIN=staging.example.com
API_SUBDOMAIN=api.example.com

# ============================================
# Staging Database - Optional
# ============================================
# Only needed if you have a separate staging environment
STAGING_DB_NAME=your_staging_database_name

# ============================================
# Security Notes
# ============================================
# 
# 1. NEVER commit .env to git
# 2. Use strong, unique passwords
# 3. Rotate credentials regularly
# 4. Use different passwords for different environments
# 5. Store production credentials securely (password manager)
# 
# See SECURITY.md for more security best practices
